/**
 * Example test scaffold — Deno-compatible.
 * Replace with real tests for your extension logic.
 */

import { assertEquals } from 'https://deno.land/std/assert/mod.ts';

Deno.test('example — sanity', () => {
  assertEquals(1 + 1, 2);
});
